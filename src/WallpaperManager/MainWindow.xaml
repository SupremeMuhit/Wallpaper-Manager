<Window
    x:Class="WallpaperManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:WallpaperManager.Models"
    Title="Wallpaper Manager">

    <Grid x:Name="RootGrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <NavigationView
            x:Name="Navigation"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            PaneTitle="Wallpaper Manager"
            SelectionChanged="Navigation_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Icon="Home" Tag="Home" IsSelected="True" />
                <NavigationViewItem Content="Library" Icon="Pictures" Tag="Library" />
                <NavigationViewItem x:Name="HiddenNavigationItem" Content="Hidden Wallpapers" Icon="Library" Tag="Hidden" Visibility="Collapsed" />
            </NavigationView.MenuItems>

            <Grid Padding="28">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid ColumnSpacing="18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="6">
                        <TextBlock x:Name="PageTitle" Text="Home" Style="{StaticResource TitleTextBlockStyle}" />
                        <TextBlock x:Name="PageSubtitle" Text="Selected wallpapers from your local Wallpaper Engine library." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Top">
                        <TextBlock x:Name="EngineStatusGlyph" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock x:Name="EngineStatusText" VerticalAlignment="Center" />
                        <Button x:Name="StartEngineButton" Content="Start Engine" Click="StartEngine_Click" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1" Margin="0,24,0,0">
                    <Grid x:Name="HomePage">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="HomeCountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                <ComboBox x:Name="HomeViewComboBox" Width="150" ItemsSource="{x:Bind LibraryViewChoices}" SelectionChanged="HomeViewComboBox_SelectionChanged" />
                                <ComboBox x:Name="HomeCardSizeComboBox" Width="120" ItemsSource="{x:Bind CardSizeChoices}" SelectionChanged="CardSizeComboBox_SelectionChanged" />
                            </StackPanel>
                        </Grid>

                        <InfoBar x:Name="EmptyHomeInfo" Grid.Row="1" Margin="0,14,0,0" Severity="Informational" IsOpen="True" Title="No selected wallpapers" Message="Use the Library item menu to add wallpapers to Home." />

                        <Grid Grid.Row="2" Margin="0,14,0,0">
                            <ListView x:Name="HomeListView" ItemsSource="{x:Bind SelectedWallpapers, Mode=OneWay}" SelectionMode="None" Visibility="Collapsed">
                                <ListView.Header>
                                    <Grid Padding="12,0,12,8" ColumnSpacing="14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="150" />
                                            <ColumnDefinition Width="220" />
                                            <ColumnDefinition Width="88" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Local name" Style="{StaticResource CaptionTextBlockStyle}" />
                                        <TextBlock Grid.Column="1" Text="ID" Style="{StaticResource CaptionTextBlockStyle}" />
                                        <TextBlock Grid.Column="2" Text="Tags" Style="{StaticResource CaptionTextBlockStyle}" />
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:WallpaperItem">
                                        <Border Background="{x:Bind RowBackground}" CornerRadius="6" Margin="0,2">
                                            <Grid Padding="12,8" ColumnSpacing="14">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="220" />
                                                    <ColumnDefinition Width="88" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Column="1" Text="{x:Bind IdText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Column="2" Text="{x:Bind TagsText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                                <Button Grid.Column="3" Content="USE" Click="RunWallpaper_Click" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>

                            <GridView x:Name="HomeThumbnailView" ItemsSource="{x:Bind SelectedWallpapers, Mode=OneWay}" SelectionMode="None">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="models:WallpaperItem">
                                        <Border Width="{x:Bind CardWidth}" Padding="12" Background="{x:Bind RowBackground}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                            <StackPanel Spacing="10">
                                                <Grid Height="{x:Bind CardPreviewHeight}">
                                                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6">
                                                        <Image Source="{x:Bind PreviewImage}" Stretch="UniformToFill" Opacity="{x:Bind PreviewOpacity}" />
                                                    </Border>
                                                    <Border Background="#99000000" CornerRadius="6" Visibility="{x:Bind NsfwOverlayVisibility}">
                                                        <TextBlock Text="NSFW" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                    </Border>
                                                </Grid>
                                                <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                                                <Grid ColumnSpacing="8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Text="{x:Bind IdText}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                                    <Button Grid.Column="1" Content="USE" Click="RunWallpaper_Click" />
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </Grid>
                    </Grid>

                    <Grid x:Name="LibraryPage" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="LibraryCountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                <Button MinHeight="36" Click="ScanLibrary_Click">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE72C;" FontSize="15" />
                                        <TextBlock Text="Scan" />
                                    </StackPanel>
                                </Button>
                                <ComboBox x:Name="LibraryViewComboBox" Width="150" ItemsSource="{x:Bind LibraryViewChoices}" SelectionChanged="LibraryViewComboBox_SelectionChanged" />
                                <ComboBox x:Name="CardSizeComboBox" Width="120" ItemsSource="{x:Bind CardSizeChoices}" SelectionChanged="CardSizeComboBox_SelectionChanged" />
                                <Button x:Name="ColumnsButton" MinHeight="36">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon Glyph="&#xE8A9;" FontSize="15" />
                                        <TextBlock Text="Columns" />
                                    </StackPanel>
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <ToggleMenuFlyoutItem x:Name="PreviewColumnToggle" Text="Preview" Tag="Preview" Click="ColumnToggle_Click" />
                                            <ToggleMenuFlyoutItem x:Name="LocalNameColumnToggle" Text="Local name" Tag="LocalName" Click="ColumnToggle_Click" />
                                            <ToggleMenuFlyoutItem x:Name="IdColumnToggle" Text="ID" Tag="Id" Click="ColumnToggle_Click" />
                                            <ToggleMenuFlyoutItem x:Name="SizeColumnToggle" Text="Size" Tag="Size" Click="ColumnToggle_Click" />
                                            <ToggleMenuFlyoutItem x:Name="TagsColumnToggle" Text="Tags" Tag="Tags" Click="ColumnToggle_Click" />
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <InfoBar x:Name="EmptyLibraryInfo" Grid.Row="1" Margin="0,14,0,0" Severity="Informational" IsOpen="True" Title="No wallpapers found" Message="Add wallpaper directories in Settings, then scan the library." />

                        <Grid Grid.Row="2" Margin="0,14,0,0">
                            <Grid x:Name="LibraryListView">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Grid Padding="12,0,12,8" ColumnSpacing="14">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="PreviewHeaderColumn" Width="104" />
                                        <ColumnDefinition x:Name="LocalNameHeaderColumn" Width="2*" />
                                        <ColumnDefinition x:Name="IdHeaderColumn" Width="140" />
                                        <ColumnDefinition x:Name="SizeHeaderColumn" Width="110" />
                                        <ColumnDefinition x:Name="TagsHeaderColumn" Width="180" />
                                        <ColumnDefinition Width="56" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="PreviewHeader" Text="Preview" Style="{StaticResource CaptionTextBlockStyle}" />
                                    <TextBlock x:Name="LocalNameHeader" Grid.Column="1" Text="Local name" Style="{StaticResource CaptionTextBlockStyle}" />
                                    <TextBlock x:Name="IdHeader" Grid.Column="2" Text="ID" Style="{StaticResource CaptionTextBlockStyle}" />
                                    <TextBlock x:Name="SizeHeader" Grid.Column="3" Text="Size" Style="{StaticResource CaptionTextBlockStyle}" />
                                    <TextBlock x:Name="TagsHeader" Grid.Column="4" Text="Tags" Style="{StaticResource CaptionTextBlockStyle}" />
                                </Grid>
                                <ListView ItemsSource="{x:Bind VisibleWallpapers, Mode=OneWay}" Grid.Row="1" SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:WallpaperItem">
                                            <Border Background="{x:Bind RowBackground}" CornerRadius="6" Margin="0,2">
                                                <Grid Padding="12,8" ColumnSpacing="14">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="{x:Bind PreviewColumnWidth}" />
                                                        <ColumnDefinition Width="{x:Bind LocalNameColumnWidth}" />
                                                        <ColumnDefinition Width="{x:Bind IdColumnWidth}" />
                                                        <ColumnDefinition Width="{x:Bind SizeColumnWidth}" />
                                                        <ColumnDefinition Width="{x:Bind TagsColumnWidth}" />
                                                        <ColumnDefinition Width="56" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid Width="88" Height="52" Visibility="{x:Bind PreviewColumnVisibility}">
                                                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4">
                                                            <Image Source="{x:Bind PreviewImage}" Stretch="UniformToFill" Opacity="{x:Bind PreviewOpacity}" />
                                                        </Border>
                                                        <Border Background="#99000000" CornerRadius="4" Visibility="{x:Bind NsfwOverlayVisibility}">
                                                            <TextBlock Text="NSFW" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold" />
                                                        </Border>
                                                    </Grid>
                                                    <TextBlock Grid.Column="1" Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Visibility="{x:Bind LocalNameColumnVisibility}" />
                                                    <TextBlock Grid.Column="2" Text="{x:Bind IdText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Visibility="{x:Bind IdColumnVisibility}" />
                                                    <TextBlock Grid.Column="3" Text="{x:Bind SizeText}" VerticalAlignment="Center" Visibility="{x:Bind SizeColumnVisibility}" />
                                                    <TextBlock Grid.Column="4" Text="{x:Bind TagsText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Visibility="{x:Bind TagsColumnVisibility}" />
                                                    <Button Grid.Column="5" Click="WallpaperActions_Click" HorizontalAlignment="Right">
                                                        <FontIcon Glyph="&#xE712;" FontSize="16" />
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </Grid>

                            <GridView x:Name="LibraryThumbnailView" ItemsSource="{x:Bind VisibleWallpapers, Mode=OneWay}" SelectionMode="None" Visibility="Collapsed">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="models:WallpaperItem">
                                        <Border Width="{x:Bind CardWidth}" Padding="12" Background="{x:Bind RowBackground}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                            <Grid>
                                                <StackPanel Spacing="10">
                                                    <Grid Height="{x:Bind CardPreviewHeight}">
                                                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6">
                                                            <Image Source="{x:Bind PreviewImage}" Stretch="UniformToFill" Opacity="{x:Bind PreviewOpacity}" />
                                                        </Border>
                                                        <Border Background="#99000000" CornerRadius="6" Visibility="{x:Bind NsfwOverlayVisibility}">
                                                            <TextBlock Text="NSFW" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                        </Border>
                                                    </Grid>
                                                    <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                                                    <TextBlock Text="{x:Bind IdText}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                                </StackPanel>
                                                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,8,0" Click="WallpaperActions_Click">
                                                    <FontIcon Glyph="&#xE712;" FontSize="16" />
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </Grid>
                    </Grid>

                    <Grid x:Name="HiddenPage" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid ColumnSpacing="16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="HiddenCountText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                <ComboBox x:Name="HiddenViewComboBox" Width="150" ItemsSource="{x:Bind LibraryViewChoices}" SelectionChanged="HiddenViewComboBox_SelectionChanged" />
                                <ComboBox x:Name="HiddenCardSizeComboBox" Width="120" ItemsSource="{x:Bind CardSizeChoices}" SelectionChanged="CardSizeComboBox_SelectionChanged" />
                            </StackPanel>
                        </Grid>
                        <Grid Grid.Row="1" Margin="0,14,0,0">
                            <ListView x:Name="HiddenListView" ItemsSource="{x:Bind HiddenWallpapers, Mode=OneWay}" SelectionMode="None">
                                <ListView.Header>
                                    <Grid Padding="12,0,12,8" ColumnSpacing="14">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="150" />
                                            <ColumnDefinition Width="220" />
                                            <ColumnDefinition Width="88" />
                                            <ColumnDefinition Width="100" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Local name" Style="{StaticResource CaptionTextBlockStyle}" />
                                        <TextBlock Grid.Column="1" Text="ID" Style="{StaticResource CaptionTextBlockStyle}" />
                                        <TextBlock Grid.Column="2" Text="Tags" Style="{StaticResource CaptionTextBlockStyle}" />
                                    </Grid>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="models:WallpaperItem">
                                        <Border Background="{x:Bind RowBackground}" CornerRadius="6" Margin="0,2">
                                            <Grid Padding="12,8" ColumnSpacing="14">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="220" />
                                                    <ColumnDefinition Width="88" />
                                                    <ColumnDefinition Width="100" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Column="1" Text="{x:Bind IdText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                                <TextBlock Grid.Column="2" Text="{x:Bind TagsText}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                                <Button Grid.Column="3" Content="USE" Click="RunWallpaper_Click" />
                                                <Button Grid.Column="4" Content="Restore" Click="RestoreWallpaper_Click" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <GridView x:Name="HiddenThumbnailView" ItemsSource="{x:Bind HiddenWallpapers, Mode=OneWay}" SelectionMode="None" Visibility="Collapsed">
                                <GridView.ItemTemplate>
                                    <DataTemplate x:DataType="models:WallpaperItem">
                                        <Border Width="{x:Bind CardWidth}" Padding="12" Background="{x:Bind RowBackground}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                            <StackPanel Spacing="10">
                                                <Grid Height="{x:Bind CardPreviewHeight}">
                                                    <Border Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="6">
                                                        <Image Source="{x:Bind PreviewImage}" Stretch="UniformToFill" Opacity="{x:Bind PreviewOpacity}" />
                                                    </Border>
                                                    <Border Background="#99000000" CornerRadius="6" Visibility="{x:Bind NsfwOverlayVisibility}">
                                                        <TextBlock Text="NSFW" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                    </Border>
                                                </Grid>
                                                <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" TextWrapping="Wrap" />
                                                <TextBlock Text="{x:Bind IdText}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" />
                                                <Grid ColumnSpacing="8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Button Content="USE" Click="RunWallpaper_Click" />
                                                    <Button Grid.Column="1" Content="Restore" Click="RestoreWallpaper_Click" />
                                                </Grid>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                            </GridView>
                        </Grid>
                    </Grid>

                    <ScrollViewer x:Name="SettingsPage" Visibility="Collapsed">
                        <StackPanel MaxWidth="820" Spacing="26" HorizontalAlignment="Left">
                            <StackPanel Spacing="10">
                                <TextBlock Text="Wallpaper Directories" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <CommandBar DefaultLabelPosition="Right" Background="Transparent">
                                    <AppBarButton Icon="Add" Label="Add directory" Click="AddDirectory_Click" />
                                </CommandBar>
                                <ListView ItemsSource="{x:Bind LibraryRoots, Mode=OneWay}" SelectionMode="None">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:WallpaperLibraryRoot">
                                            <Grid Padding="0,8" ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <FontIcon Glyph="&#xE8B7;" />
                                                <StackPanel Grid.Column="1" Spacing="2">
                                                    <TextBlock Text="{x:Bind DisplayName}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                    <TextBlock Text="{x:Bind Path}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                                </StackPanel>
                                                <Button Grid.Column="2" Content="Remove" Tag="{x:Bind Path}" Click="RemoveDirectory_Click" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>

                            <StackPanel Spacing="10">
                                <TextBlock Text="Engine Executable Location" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="EnginePathTextBox" PlaceholderText="wallpaper32.exe or wallpaper64.exe" TextChanged="EnginePathTextBox_TextChanged" />
                                    <Button Grid.Column="1" Content="Browse" Click="BrowseEngineExecutable_Click" />
                                </Grid>
                            </StackPanel>

                            <StackPanel Spacing="10">
                                <TextBlock Text="Theme" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <ComboBox x:Name="ThemeComboBox" Width="220" ItemsSource="{x:Bind ThemeChoices}" SelectionChanged="ThemeComboBox_SelectionChanged" />
                            </StackPanel>

                            <StackPanel Spacing="10">
                                <TextBlock Text="Hidden Wallpapers" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <ToggleSwitch x:Name="ShowHiddenPageToggle" Header="Show Hidden Wallpapers page" Toggled="ShowHiddenPageToggle_Toggled" />
                                <Button Content="Change hidden page password" Click="ChangeHiddenPassword_Click" />
                            </StackPanel>

                            <StackPanel Spacing="12">
                                <TextBlock Text="Tags" Style="{StaticResource SubtitleTextBlockStyle}" />
                                <ToggleSwitch x:Name="ColorRowsToggle" Header="Color wallpaper rows by highest priority tag" Toggled="ColorRowsToggle_Toggled" />
                                <Grid ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="260" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox x:Name="NewTagNameTextBox" PlaceholderText="Tag name" />
                                    <Button Grid.Column="1" Content="Create" Click="CreateTag_Click" />
                                </Grid>
                                <ListView x:Name="TagsListView" ItemsSource="{x:Bind Tags, Mode=OneWay}" SelectionMode="None" CanDragItems="True" CanReorderItems="True" AllowDrop="True" DragItemsCompleted="TagsListView_DragItemsCompleted">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="models:WallpaperTag">
                                            <Grid Padding="0,8" ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="28" />
                                                    <ColumnDefinition Width="28" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="⠿" FontSize="18" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
                                                <Border Grid.Column="1" Width="18" Height="18" CornerRadius="4" Background="{x:Bind ColorBrush}" />
                                                <StackPanel Grid.Column="2" Spacing="2">
                                                    <TextBlock Text="{x:Bind Name}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                                    <TextBlock Text="{x:Bind Color}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                                </StackPanel>
                                                <Button Grid.Column="3" Content="Color" Click="EditTagColor_Click" />
                                                <Button Grid.Column="4" Content="Remove" Click="RemoveTag_Click" />
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
